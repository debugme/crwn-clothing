import { FunctionComponent } from 'react'

import { Homepage } from './pages/homepage/Homepage'

interface AppProps {}

export const App: FunctionComponent<AppProps> = (): JSX.Element => <Homepage />
